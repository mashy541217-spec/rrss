/** Priority level for Execution scheduling and queue admission. */
export enum ExecutionPriority {
  Critical = 'Critical',
  High = 'High',
  Standard = 'Standard',
  Low = 'Low',
  Background = 'Background',
}
