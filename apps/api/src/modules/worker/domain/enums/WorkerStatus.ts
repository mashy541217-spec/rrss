export enum WorkerStatus {
  Registered = 'registered',
  Starting = 'starting',
  Idle = 'idle',
  Busy = 'busy',
  Paused = 'paused',
  Draining = 'draining',
  Offline = 'offline',
  Terminated = 'terminated'
}
