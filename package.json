{
  "name": "rrss-auto",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "apps/studio"
  ],
  "scripts": {
    "test": "jest",
    "build:plugin-sdk": "npm run build --workspace=packages/plugin-sdk",
    "build:plugin-instagram": "npm run build --workspace=packages/plugin-instagram",
    "build:meta-sdk": "npm run build --workspace=packages/meta-sdk",
    "build:plugin-facebook": "npm run build --workspace=packages/plugin-facebook",
    "build:plugin-threads": "npm run build --workspace=packages/plugin-threads",
    "build:plugin-telegram": "npm run build --workspace=packages/plugin-telegram",
    "build:plugin-whatsapp-business": "npm run build --workspace=packages/plugin-whatsapp-business",
    "build:plugin-messenger": "npm run build --workspace=packages/plugin-messenger",
    "build:plugin-discord": "npm run build --workspace=packages/plugin-discord",
    "build:plugin-slack": "npm run build --workspace=packages/plugin-slack",
    "build:browser-sdk": "npm run build --workspace=packages/browser-sdk",
    "build:browser-engine-sdk": "npm run build --workspace=packages/browser-engine-sdk",
    "build:browser-engine-playwright": "npm run build --workspace=packages/browser-engine-playwright",
    "build:enterprise-web-toolkit": "npm run build --workspace=packages/enterprise-web-toolkit",
    "build:plugin-dealernet": "npm run build --workspace=packages/plugin-dealernet",
    "build:studio": "npm run build --workspace=apps/studio",
    "build:admin-web": "npm run build --workspace=apps/admin-web",
    "build:social-sdk": "npm run build --workspace=packages/social-sdk",
    "build:messaging-sdk": "npm run build --workspace=packages/messaging-sdk",
    "build:domain": "npm run build --workspace=packages/domain",
    "build:application": "npm run build --workspace=packages/application",
    "build:infrastructure": "npm run build --workspace=packages/infrastructure",
    "build:testing": "npm run build --workspace=packages/testing",
    "build:logger": "npm run build --workspace=packages/logger",
    "build:configuration": "npm run build --workspace=packages/configuration",
    "build:observability": "npm run build --workspace=packages/observability"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "jest": "^29.7.0",
    "rimraf": "^5.0.5",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "axios": "^1.18.1"
  }
}
